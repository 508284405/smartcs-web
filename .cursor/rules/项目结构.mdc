---
description: 
globs: 
alwaysApply: false
---
# 项目结构说明

本项目为多模块 Java 项目，采用 Maven 进行模块化管理，主要包含以下子模块：

- [smartcs-web-adapter](mdc:smartcs-web-adapter)
- [smartcs-web-app](mdc:smartcs-web-app)
- [smartcs-web-domain](mdc:smartcs-web-domain)
- [smartcs-web-client](mdc:smartcs-web-client)
- [smartcs-web-infrastructure](mdc:smartcs-web-infrastructure)
- [start](mdc:start)

## 各模块结构

### smartcs-web-app
- 主要业务逻辑模块，包含如下目录：
  - `src/main/java/com/leyue/smartcs/`：主业务包，包含 `user`、`customer`、`order` 等子包，每个子包下有 `executor` 等子目录。
  - `src/test/java/com/leyue/smartcs/app/`：测试代码。
  - `pom.xml`：模块依赖与构建配置。

### smartcs-web-adapter
- 适配层模块，结构类似，包含 `src` 目录和 `pom.xml`。

### smartcs-web-domain
- 领域层模块，结构类似，包含 `src` 目录和 `pom.xml`。

### smartcs-web-client
- 客户端相关模块，结构类似，包含 `src` 目录和 `pom.xml`。

### smartcs-web-infrastructure
- 基础设施层模块，结构类似，包含 `src` 目录和 `pom.xml`。

### start
- 启动模块，包含 `src` 目录和 `pom.xml`。

## 其他
- 根目录下有 [pom.xml](mdc:pom.xml) 为父级聚合配置。
- 其他如 `.gitignore`、`.idea/`、`.vscode/` 等为工程配置文件夹。

如需了解具体包结构，可进一步参考各模块下的 `src/main/java` 目录。
