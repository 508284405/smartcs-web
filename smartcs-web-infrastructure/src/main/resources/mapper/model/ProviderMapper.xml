<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leyue.smartcs.model.mapper.ProviderMapper">

    <!-- 根据提供商标识查询 -->
    <select id="selectByProviderKey" resultType="com.leyue.smartcs.model.dataobject.ProviderDO">
        SELECT *
        FROM t_model_provider
        WHERE provider_key = #{providerKey}
        AND is_deleted = 0
    </select>

    <!-- 检查提供商标识是否已存在 -->
    <select id="countByProviderKey" resultType="int">
        SELECT COUNT(*)
        FROM t_model_provider
        WHERE provider_key = #{providerKey}
        AND is_deleted = 0
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

</mapper>